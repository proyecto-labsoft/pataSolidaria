spring:
  springdoc:
    api-docs:
      enabled = true
    swagger-ui:
      enabled = true
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db_araf_V2
    username: josegaray
    password: 5404822
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  server:
    port: 8083
  logging:
    level:
      root: INFO
      org.hibernate: DEBUG
      org.hibernate.SQL: DEBUG
      org.hibernate.descriptor.sql: TRUE
  main:
    allow-bean-definition-overriding: true